# Ignorer les dossiers node_modules
server/node_modules
client/node_modules

# Ignorer les dossiers build
**/build

# Ignorer les fichiers de configuration spécifiques à l'environnement (s'ils existent)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ignorer les fichiers de cache ou de logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Ignorer les fichiers .env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ignorer les fichiers .DS_Store (spécifiques à macOS)
.DS_Store

# Ignorer les fichiers de logs
logs
*.log

# Ignorer les fichiers .idea (spécifiques à JetBrains)
.idea

# Ignorer les fichiers .vscode (spécifiques à Visual Studio Code)
.vscode
*.code-workspace

# Ignorer les fichiers de cache de ESLint
.eslintcache

# Ignorer les fichiers .git (répertoire .git spécifique à Git)
.git

# Ignorer les fichiers .idea (spécifiques à JetBrains)
.idea

# Ignorer les fichiers .vscode (spécifiques à Visual Studio Code)
.vscode
*.code-workspace

# Ignorer les fichiers de cache de ESLint
.eslintcache

# Ignorer les fichiers .git (répertoire .git spécifique à Git)
.git
